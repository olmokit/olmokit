{
  "name": "laravel-frontend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/laravel-frontend/src",
  "projectType": "library",
  "implicitDependencies": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "inputs": [
          {
            "externalDependencies": []
          }
        ],
        "commands": [
          "mkdir -p ./packages/laravel-frontend/dist",
          "rsync -a ./packages/laravel-frontend/* ./packages/laravel-frontend/dist --exclude dist --exclude vendor --exclude project.json --exclude .gitignore"
        ],
        "parallel": false
      }
    }
  },
  "tags": []
}
