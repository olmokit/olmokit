{
  "name": "template-laravel",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/template-laravel/src",
  "projectType": "library",
  "implicitDependencies": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "inputs": [
          {
            "externalDependencies": []
          }
        ],
        "commands": [
          "mkdir -p ./packages/template-laravel/dist",
          "rsync -a ./packages/template-laravel/* ./packages/template-laravel/dist --exclude dist --exclude project.json"
        ],
        "parallel": false
      }
    }
  },
  "tags": []
}
